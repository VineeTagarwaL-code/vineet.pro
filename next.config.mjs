/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.discordapp.com','i.scdn.co'], // Add 'cdn.discordapp.com' to the list of allowed domains
      },
      
      
};

export default nextConfig;
